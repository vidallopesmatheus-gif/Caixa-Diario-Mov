# Chat com IA — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar um assistente de IA com chat ao Caixa Diário, acessível via botão flutuante em todas as páginas autenticadas, com guard-rails que limitam as respostas ao escopo do app.

**Architecture:** Backend expõe `POST /api/chat` (JWT-protected) que executa pré-filtro por regex (`TopicGuard`) antes de chamar Claude Haiku 3.5 via `Anthropic.SDK`. O frontend adiciona `ChatButton` + `ChatPanel` ao `Layout.tsx`, com histórico stateless gerenciado por `useChat`.

**Tech Stack:** .NET 10, `Anthropic.SDK` NuGet, xUnit + Moq (testes backend); React + TypeScript, Vitest (testes frontend).

---

## Mapa de arquivos

### Backend — novos
| Arquivo | Responsabilidade |
|---|---|
| `CaixaDiario.API/DTOs/Chat/ChatMessageDto.cs` | DTO de uma mensagem (role + content) |
| `CaixaDiario.API/DTOs/Chat/ChatRequestDto.cs` | DTO do request (mensagem atual + histórico) |
| `CaixaDiario.API/DTOs/Chat/ChatResponseDto.cs` | DTO do response (reply + wasBlocked) |
| `CaixaDiario.API/Services/TopicGuard.cs` | Pré-filtro stateless por regex |
| `CaixaDiario.API/Services/IAnthropicClient.cs` | Interface mockável para o SDK Anthropic |
| `CaixaDiario.API/Services/AnthropicClientWrapper.cs` | Wrapper sobre `Anthropic.SDK` |
| `CaixaDiario.API/Services/IChatService.cs` | Interface do serviço de chat |
| `CaixaDiario.API/Services/ChatService.cs` | Orquestra guard + chamada à IA |
| `CaixaDiario.API/Controllers/ChatController.cs` | `POST /api/chat` com `[Authorize]` |
| `CaixaDiario.Tests/Services/TopicGuardTests.cs` | Testes unitários do pré-filtro |
| `CaixaDiario.Tests/Services/ChatServiceTests.cs` | Testes unitários do ChatService |

### Backend — modificados
| Arquivo | Alteração |
|---|---|
| `CaixaDiario.API/appsettings.json` | Adicionar seção `"Anthropic"` |
| `CaixaDiario.API/Program.cs` | Registrar `IChatService`, `IAnthropicClient` |

### Frontend — novos
| Arquivo | Responsabilidade |
|---|---|
| `frontend/src/api/chat.ts` | Chamada `POST /api/chat` via `apiFetch` |
| `frontend/src/api/chat.test.ts` | Testes da função de API |
| `frontend/src/hooks/useChat.ts` | Estado de mensagens em memória |
| `frontend/src/components/Chat/ChatButton.tsx` | Botão flutuante |
| `frontend/src/components/Chat/ChatPanel.tsx` | Painel deslizante com input |
| `frontend/src/components/Chat/Chat.css` | Estilos do chat |

### Frontend — modificados
| Arquivo | Alteração |
|---|---|
| `frontend/src/types.ts` | Adicionar `ChatMessage` e `ChatResponse` |
| `frontend/src/components/Layout/Layout.tsx` | Adicionar `<ChatButton>` e `<ChatPanel>` |

---

## Task 1: Instalar Anthropic.SDK e configurar appsettings

**Files:**
- Modify: `CaixaDiario.API/CaixaDiario.API.csproj`
- Modify: `CaixaDiario.API/appsettings.json`

- [ ] **Step 1: Instalar o pacote NuGet**

```bash
dotnet add CaixaDiario.API/CaixaDiario.API.csproj package Anthropic.SDK
```

Expected: linha `<PackageReference Include="Anthropic.SDK" ...>` adicionada ao `.csproj`.

- [ ] **Step 2: Adicionar seção Anthropic no appsettings.json**

Arquivo atual (`CaixaDiario.API/appsettings.json`):
```json
{
  "Logging": { "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" } },
  "AllowedHosts": "*",
  "ConnectionStrings": { "DefaultConnection": "" },
  "Jwt": { "SecretKey": "", "Issuer": "CaixaDiario", "Audience": "CaixaDiario", "ExpiresInHours": "24" },
  "Cors": { "AllowedOrigins": "" }
}
```

Substituir por:
```json
{
  "Logging": { "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" } },
  "AllowedHosts": "*",
  "ConnectionStrings": { "DefaultConnection": "" },
  "Jwt": { "SecretKey": "", "Issuer": "CaixaDiario", "Audience": "CaixaDiario", "ExpiresInHours": "24" },
  "Cors": { "AllowedOrigins": "" },
  "Anthropic": {
    "ApiKey": "",
    "Model": "claude-haiku-4-5-20251001",
    "MaxTokens": 1024,
    "HistoricoMaxMensagens": 10
  }
}
```

- [ ] **Step 3: Adicionar ANTHROPIC_API_KEY no .env**

Abrir `.env` na raiz do projeto e adicionar:
```
ANTHROPIC_API_KEY=sua-chave-aqui
```

> A chave real deve ser obtida em https://console.anthropic.com/

- [ ] **Step 4: Commit**

```bash
git add CaixaDiario.API/CaixaDiario.API.csproj CaixaDiario.API/appsettings.json
git commit -m "chore: add Anthropic.SDK package and configure appsettings"
```

---

## Task 2: Criar DTOs de Chat

**Files:**
- Create: `CaixaDiario.API/DTOs/Chat/ChatMessageDto.cs`
- Create: `CaixaDiario.API/DTOs/Chat/ChatRequestDto.cs`
- Create: `CaixaDiario.API/DTOs/Chat/ChatResponseDto.cs`

- [ ] **Step 1: Criar ChatMessageDto.cs**

```csharp
// CaixaDiario.API/DTOs/Chat/ChatMessageDto.cs
namespace CaixaDiario.API.DTOs.Chat;

public class ChatMessageDto
{
    public string Role { get; set; } = string.Empty;    // "user" ou "assistant"
    public string Content { get; set; } = string.Empty;
}
```

- [ ] **Step 2: Criar ChatRequestDto.cs**

```csharp
// CaixaDiario.API/DTOs/Chat/ChatRequestDto.cs
namespace CaixaDiario.API.DTOs.Chat;

public class ChatRequestDto
{
    public string Message { get; set; } = string.Empty;
    public List<ChatMessageDto> History { get; set; } = [];
}
```

- [ ] **Step 3: Criar ChatResponseDto.cs**

```csharp
// CaixaDiario.API/DTOs/Chat/ChatResponseDto.cs
namespace CaixaDiario.API.DTOs.Chat;

public class ChatResponseDto
{
    public string Reply { get; set; } = string.Empty;
    public bool WasBlocked { get; set; }
}
```

- [ ] **Step 4: Commit**

```bash
git add CaixaDiario.API/DTOs/Chat/
git commit -m "feat: add Chat DTOs"
```

---

## Task 3: Implementar TopicGuard com TDD

**Files:**
- Create: `CaixaDiario.API/Services/TopicGuard.cs`
- Create: `CaixaDiario.Tests/Services/TopicGuardTests.cs`

- [ ] **Step 1: Escrever os testes**

```csharp
// CaixaDiario.Tests/Services/TopicGuardTests.cs
using CaixaDiario.API.Services;

namespace CaixaDiario.Tests.Services;

public class TopicGuardTests
{
    [Theory]
    [InlineData("me dê uma receita de bolo")]
    [InlineData("qual é a receita de frango assado?")]
    [InlineData("previsão do tempo para amanhã")]
    [InlineData("qual o clima em São Paulo hoje?")]
    [InlineData("temperatura em Recife")]
    [InlineData("quem ganhou o futebol ontem?")]
    [InlineData("me fale sobre política")]
    [InlineData("quais são as últimas notícias?")]
    [InlineData("como instalar o Python?")]
    [InlineData("tutorial de programação em C#")]
    public void IsOffTopic_MensagensForaDoEscopo_RetornaTrue(string mensagem)
    {
        var resultado = TopicGuard.IsOffTopic(mensagem);
        Assert.True(resultado);
    }

    [Theory]
    [InlineData("como adiciono uma entrada no caixa?")]
    [InlineData("o que é uma conta provisionada?")]
    [InlineData("como exportar meus dados?")]
    [InlineData("como funciona a meta anual?")]
    [InlineData("qual é o saldo do dashboard?")]
    [InlineData("como ver o histórico de registros?")]
    [InlineData("não entendo o gráfico de gastos")]
    [InlineData("como faço login?")]
    [InlineData("o que são saídas no caixa diário?")]
    public void IsOffTopic_MensagensDentroDoEscopo_RetornaFalse(string mensagem)
    {
        var resultado = TopicGuard.IsOffTopic(mensagem);
        Assert.False(resultado);
    }

    [Theory]
    [InlineData("")]
    [InlineData("   ")]
    public void IsOffTopic_MensagemVazia_RetornaFalse(string mensagem)
    {
        var resultado = TopicGuard.IsOffTopic(mensagem);
        Assert.False(resultado);
    }
}
```

- [ ] **Step 2: Rodar os testes e confirmar que falham**

```bash
dotnet test CaixaDiario.Tests --filter "TopicGuardTests" --no-build 2>&1 | tail -5
```

Expected: erro de compilação — `TopicGuard` não existe ainda.

- [ ] **Step 3: Implementar TopicGuard.cs**

```csharp
// CaixaDiario.API/Services/TopicGuard.cs
using System.Text.RegularExpressions;

namespace CaixaDiario.API.Services;

public static class TopicGuard
{
    private static readonly Regex[] _blockedPatterns =
    [
        new(@"receita(s)?\s+de\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\bbolo\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\bculin[aá]ria\b|\bcozinha\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"previs[aã]o\s+do\s+tempo", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\bclima\s+em\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\btemperatura\s+em\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\bfutebol\b|\bbasquete\b|\bv[oô]lei\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\bpol[ií]tica\b|\belei[cç][aã]o\b|\bpresidente\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"\bnotícia(s)?\b|\bjornal\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"como\s+instalar\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"tutorial\s+de\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
        new(@"programar\s+em\b", RegexOptions.IgnoreCase | RegexOptions.Compiled),
    ];

    public static bool IsOffTopic(string message)
    {
        if (string.IsNullOrWhiteSpace(message)) return false;
        return _blockedPatterns.Any(p => p.IsMatch(message));
    }
}
```

- [ ] **Step 4: Rodar os testes e confirmar que passam**

```bash
dotnet test CaixaDiario.Tests --filter "TopicGuardTests" -v minimal
```

Expected: todos os testes `TopicGuardTests` passando (18 testes).

- [ ] **Step 5: Commit**

```bash
git add CaixaDiario.API/Services/TopicGuard.cs CaixaDiario.Tests/Services/TopicGuardTests.cs
git commit -m "feat: add TopicGuard with regex pre-filter and tests"
```

---

## Task 4: Criar IAnthropicClient e AnthropicClientWrapper

**Files:**
- Create: `CaixaDiario.API/Services/IAnthropicClient.cs`
- Create: `CaixaDiario.API/Services/AnthropicClientWrapper.cs`

- [ ] **Step 1: Criar IAnthropicClient.cs**

```csharp
// CaixaDiario.API/Services/IAnthropicClient.cs
using CaixaDiario.API.DTOs.Chat;

namespace CaixaDiario.API.Services;

public interface IAnthropicClient
{
    Task<string> EnviarMensagemAsync(string systemPrompt, List<ChatMessageDto> historico, string mensagem);
}
```

- [ ] **Step 2: Criar AnthropicClientWrapper.cs**

```csharp
// CaixaDiario.API/Services/AnthropicClientWrapper.cs
using Anthropic.SDK;
using Anthropic.SDK.Messaging;
using CaixaDiario.API.DTOs.Chat;

namespace CaixaDiario.API.Services;

public class AnthropicClientWrapper : IAnthropicClient
{
    private readonly AnthropicClient _client;
    private readonly string _model;
    private readonly int _maxTokens;

    public AnthropicClientWrapper(string apiKey, string model, int maxTokens)
    {
        _client = new AnthropicClient(apiKey);
        _model = model;
        _maxTokens = maxTokens;
    }

    public async Task<string> EnviarMensagemAsync(string systemPrompt, List<ChatMessageDto> historico, string mensagem)
    {
        var messages = historico
            .Select(m => new Message
            {
                Role = m.Role == "user" ? RoleType.User : RoleType.Assistant,
                Content = m.Content
            })
            .ToList();

        messages.Add(new Message { Role = RoleType.User, Content = mensagem });

        var parameters = new MessageParameters
        {
            Messages = messages,
            Model = _model,
            MaxTokens = _maxTokens,
            System = systemPrompt
        };

        var response = await _client.Messages.GetClaudeMessageAsync(parameters);
        return response.Content.FirstOrDefault()?.ToString() ?? string.Empty;
    }
}
```

- [ ] **Step 3: Commit**

```bash
git add CaixaDiario.API/Services/IAnthropicClient.cs CaixaDiario.API/Services/AnthropicClientWrapper.cs
git commit -m "feat: add IAnthropicClient interface and AnthropicClientWrapper"
```

---

## Task 5: Implementar IChatService e ChatService com TDD

**Files:**
- Create: `CaixaDiario.API/Services/IChatService.cs`
- Create: `CaixaDiario.API/Services/ChatService.cs`
- Create: `CaixaDiario.Tests/Services/ChatServiceTests.cs`

- [ ] **Step 1: Criar IChatService.cs**

```csharp
// CaixaDiario.API/Services/IChatService.cs
using CaixaDiario.API.DTOs.Chat;

namespace CaixaDiario.API.Services;

public interface IChatService
{
    Task<ChatResponseDto> ResponderAsync(ChatRequestDto dto);
}
```

- [ ] **Step 2: Escrever os testes de ChatService**

```csharp
// CaixaDiario.Tests/Services/ChatServiceTests.cs
using CaixaDiario.API.DTOs.Chat;
using CaixaDiario.API.Services;
using Moq;

namespace CaixaDiario.Tests.Services;

public class ChatServiceTests
{
    private readonly Mock<IAnthropicClient> _clientMock = new();
    private readonly ChatService _sut;

    public ChatServiceTests()
    {
        _sut = new ChatService(_clientMock.Object);
    }

    [Fact]
    public async Task ResponderAsync_MensagemOffTopic_RetornaRespostaBloqueadaSemChamarAPI()
    {
        var dto = new ChatRequestDto { Message = "me dê uma receita de bolo", History = [] };

        var resultado = await _sut.ResponderAsync(dto);

        Assert.True(resultado.WasBlocked);
        Assert.Contains("Caixa Diário", resultado.Reply);
        _clientMock.Verify(c => c.EnviarMensagemAsync(It.IsAny<string>(), It.IsAny<List<ChatMessageDto>>(), It.IsAny<string>()), Times.Never);
    }

    [Fact]
    public async Task ResponderAsync_MensagemValida_ChamaAPIERetornaResposta()
    {
        var dto = new ChatRequestDto
        {
            Message = "como adiciono uma entrada no caixa?",
            History = []
        };
        _clientMock
            .Setup(c => c.EnviarMensagemAsync(It.IsAny<string>(), It.IsAny<List<ChatMessageDto>>(), dto.Message))
            .ReturnsAsync("Na página Caixa, clique em adicionar entrada.");

        var resultado = await _sut.ResponderAsync(dto);

        Assert.False(resultado.WasBlocked);
        Assert.Equal("Na página Caixa, clique em adicionar entrada.", resultado.Reply);
        _clientMock.Verify(c => c.EnviarMensagemAsync(It.IsAny<string>(), It.IsAny<List<ChatMessageDto>>(), dto.Message), Times.Once);
    }

    [Fact]
    public async Task ResponderAsync_HistoricoComMaisDe10Mensagens_EnviaApenasUltimas10()
    {
        var historico = Enumerable.Range(1, 15)
            .Select(i => new ChatMessageDto { Role = "user", Content = $"msg {i}" })
            .ToList();
        var dto = new ChatRequestDto { Message = "como exportar?", History = historico };

        _clientMock
            .Setup(c => c.EnviarMensagemAsync(It.IsAny<string>(), It.IsAny<List<ChatMessageDto>>(), dto.Message))
            .ReturnsAsync("resposta");

        await _sut.ResponderAsync(dto);

        _clientMock.Verify(c => c.EnviarMensagemAsync(
            It.IsAny<string>(),
            It.Is<List<ChatMessageDto>>(h => h.Count == 10),
            dto.Message), Times.Once);
    }

    [Fact]
    public async Task ResponderAsync_MensagemVazia_ChamaAPINormalmente()
    {
        var dto = new ChatRequestDto { Message = "", History = [] };
        _clientMock
            .Setup(c => c.EnviarMensagemAsync(It.IsAny<string>(), It.IsAny<List<ChatMessageDto>>(), dto.Message))
            .ReturnsAsync("Como posso ajudar?");

        var resultado = await _sut.ResponderAsync(dto);

        Assert.False(resultado.WasBlocked);
    }
}
```

- [ ] **Step 3: Rodar os testes e confirmar que falham**

```bash
dotnet test CaixaDiario.Tests --filter "ChatServiceTests" --no-build 2>&1 | tail -5
```

Expected: erro de compilação — `ChatService` não existe ainda.

- [ ] **Step 4: Implementar ChatService.cs**

```csharp
// CaixaDiario.API/Services/ChatService.cs
using CaixaDiario.API.DTOs.Chat;

namespace CaixaDiario.API.Services;

public class ChatService : IChatService
{
    private const string SystemPrompt =
        """
        Você é o assistente do Caixa Diário, um aplicativo de controle financeiro pessoal.
        Você responde APENAS perguntas relacionadas a:
        - Como usar o app (registros diários, entradas, saídas)
        - Contas provisionadas
        - Metas anuais
        - Dashboard e gráficos
        - Exportação de dados
        - Dúvidas sobre finanças pessoais básicas no contexto do app

        Se o usuário perguntar qualquer coisa fora desse escopo, responda:
        "Só posso ajudar com o Caixa Diário. Tem alguma dúvida sobre registros, metas ou contas?"

        Seja direto e conciso. Responda sempre em português.
        """;

    private const string RespostaBloqueada =
        "Só consigo ajudar com perguntas sobre o Caixa Diário. " +
        "Tente perguntar sobre registros, metas, contas ou como usar o app.";

    private const int MaxHistorico = 10;

    private readonly IAnthropicClient _anthropicClient;

    public ChatService(IAnthropicClient anthropicClient)
    {
        _anthropicClient = anthropicClient;
    }

    public async Task<ChatResponseDto> ResponderAsync(ChatRequestDto dto)
    {
        if (TopicGuard.IsOffTopic(dto.Message))
            return new ChatResponseDto { Reply = RespostaBloqueada, WasBlocked = true };

        var historico = dto.History.TakeLast(MaxHistorico).ToList();
        var reply = await _anthropicClient.EnviarMensagemAsync(SystemPrompt, historico, dto.Message);

        return new ChatResponseDto { Reply = reply, WasBlocked = false };
    }
}
```

- [ ] **Step 5: Rodar os testes e confirmar que passam**

```bash
dotnet test CaixaDiario.Tests --filter "ChatServiceTests" -v minimal
```

Expected: todos os 4 testes passando.

- [ ] **Step 6: Commit**

```bash
git add CaixaDiario.API/Services/IChatService.cs CaixaDiario.API/Services/ChatService.cs CaixaDiario.Tests/Services/ChatServiceTests.cs
git commit -m "feat: implement ChatService with topic guard and history limit"
```

---

## Task 6: Criar ChatController e registrar serviços

**Files:**
- Create: `CaixaDiario.API/Controllers/ChatController.cs`
- Modify: `CaixaDiario.API/Program.cs`

- [ ] **Step 1: Criar ChatController.cs**

```csharp
// CaixaDiario.API/Controllers/ChatController.cs
using CaixaDiario.API.DTOs.Chat;
using CaixaDiario.API.Responses;
using CaixaDiario.API.Services;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;

namespace CaixaDiario.API.Controllers;

[ApiController]
[Route("api/chat")]
[Authorize]
public class ChatController : ControllerBase
{
    private readonly IChatService _chatService;

    public ChatController(IChatService chatService) => _chatService = chatService;

    [HttpPost]
    public async Task<IActionResult> Responder([FromBody] ChatRequestDto dto)
    {
        var resultado = await _chatService.ResponderAsync(dto);
        return Ok(new ApiResponse<ChatResponseDto> { Dados = resultado });
    }
}
```

- [ ] **Step 2: Registrar serviços no Program.cs**

Adicionar as linhas a seguir em `CaixaDiario.API/Program.cs`, logo após o bloco de injeção de dependência existente (depois da linha `builder.Services.AddScoped<IMetaService, MetaService>();`):

```csharp
// Chat IA
var anthropicApiKey = builder.Configuration["Anthropic:ApiKey"]
    ?? Environment.GetEnvironmentVariable("ANTHROPIC_API_KEY")
    ?? throw new InvalidOperationException("ANTHROPIC_API_KEY não configurada.");
var anthropicModel = builder.Configuration["Anthropic:Model"] ?? "claude-haiku-4-5-20251001";
var anthropicMaxTokens = int.TryParse(builder.Configuration["Anthropic:MaxTokens"], out var mt) ? mt : 1024;
builder.Services.AddSingleton<IAnthropicClient>(
    new AnthropicClientWrapper(anthropicApiKey, anthropicModel, anthropicMaxTokens));
builder.Services.AddScoped<IChatService, ChatService>();
```

- [ ] **Step 3: Compilar e confirmar sem erros**

```bash
dotnet build CaixaDiario.API/CaixaDiario.API.csproj -c Debug 2>&1 | tail -5
```

Expected: `Build succeeded.`

- [ ] **Step 4: Rodar todos os testes do backend**

```bash
dotnet test CaixaDiario.Tests -v minimal
```

Expected: todos os testes passando.

- [ ] **Step 5: Commit**

```bash
git add CaixaDiario.API/Controllers/ChatController.cs CaixaDiario.API/Program.cs
git commit -m "feat: add ChatController and register chat services in DI"
```

---

## Task 7: Adicionar tipos de chat ao frontend

**Files:**
- Modify: `frontend/src/types.ts`

- [ ] **Step 1: Adicionar tipos ChatMessage e ChatResponse ao final de types.ts**

Ao final do arquivo `frontend/src/types.ts`, adicionar:

```typescript
export interface ChatMessage {
  role: 'user' | 'assistant'
  content: string
}

export interface ChatResponse {
  dados: {
    reply: string
    wasBlocked: boolean
  }
  codigoRetorno: string
  mensagem: string
}
```

- [ ] **Step 2: Confirmar que o TypeScript não gerou erros**

```bash
cd frontend && npx tsc --noEmit 2>&1 | head -20
```

Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/types.ts
git commit -m "feat: add ChatMessage and ChatResponse types"
```

---

## Task 8: Criar chat.ts (camada de API) com TDD

**Files:**
- Create: `frontend/src/api/chat.ts`
- Create: `frontend/src/api/chat.test.ts`

- [ ] **Step 1: Escrever os testes**

```typescript
// frontend/src/api/chat.test.ts
import { enviarMensagem } from './chat'

beforeEach(() => {
  vi.stubGlobal('fetch', vi.fn())
  localStorage.setItem('token', 'jwt-test')
})

afterEach(() => {
  vi.unstubAllGlobals()
  localStorage.clear()
})

test('enviarMensagem faz POST em /api/chat', async () => {
  vi.mocked(fetch).mockResolvedValue({
    ok: true,
    status: 200,
    json: async () => ({ dados: { reply: 'Olá!', wasBlocked: false }, codigoRetorno: 'SUCESSO', mensagem: '' }),
  } as Response)

  await enviarMensagem('como uso o app?', [])

  expect(vi.mocked(fetch)).toHaveBeenCalledWith(
    '/api/chat',
    expect.objectContaining({ method: 'POST' })
  )
})

test('enviarMensagem envia message e history no body', async () => {
  vi.mocked(fetch).mockResolvedValue({
    ok: true,
    status: 200,
    json: async () => ({ dados: { reply: 'ok', wasBlocked: false }, codigoRetorno: 'SUCESSO', mensagem: '' }),
  } as Response)

  const historico = [{ role: 'user' as const, content: 'oi' }]
  await enviarMensagem('como exportar?', historico)

  const call = vi.mocked(fetch).mock.calls[0][1] as RequestInit
  const body = JSON.parse(call.body as string)
  expect(body.message).toBe('como exportar?')
  expect(body.history).toEqual(historico)
})

test('enviarMensagem inclui Authorization header', async () => {
  vi.mocked(fetch).mockResolvedValue({
    ok: true,
    status: 200,
    json: async () => ({ dados: { reply: 'ok', wasBlocked: false }, codigoRetorno: 'SUCESSO', mensagem: '' }),
  } as Response)

  await enviarMensagem('oi', [])

  const call = vi.mocked(fetch).mock.calls[0][1] as RequestInit
  expect((call.headers as Record<string, string>)['Authorization']).toBe('Bearer jwt-test')
})
```

- [ ] **Step 2: Rodar os testes e confirmar que falham**

```bash
cd frontend && npx vitest run src/api/chat.test.ts 2>&1 | tail -10
```

Expected: erro — módulo `./chat` não encontrado.

- [ ] **Step 3: Criar chat.ts**

```typescript
// frontend/src/api/chat.ts
import { apiFetch } from './client'
import type { ChatMessage, ChatResponse } from '../types'

export const enviarMensagem = (
  message: string,
  history: ChatMessage[]
): Promise<ChatResponse> =>
  apiFetch<ChatResponse>('/api/chat', {
    method: 'POST',
    body: JSON.stringify({ message, history }),
  })
```

- [ ] **Step 4: Rodar os testes e confirmar que passam**

```bash
cd frontend && npx vitest run src/api/chat.test.ts 2>&1 | tail -10
```

Expected: 3 testes passando.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/api/chat.ts frontend/src/api/chat.test.ts
git commit -m "feat: add chat API function with tests"
```

---

## Task 9: Criar useChat hook

**Files:**
- Create: `frontend/src/hooks/useChat.ts`

- [ ] **Step 1: Criar useChat.ts**

```typescript
// frontend/src/hooks/useChat.ts
import { useState, useCallback } from 'react'
import { enviarMensagem } from '../api/chat'
import type { ChatMessage } from '../types'

export function useChat() {
  const [messages, setMessages] = useState<ChatMessage[]>([])
  const [isLoading, setIsLoading] = useState(false)

  const sendMessage = useCallback(async (text: string) => {
    if (!text.trim() || isLoading) return

    const userMsg: ChatMessage = { role: 'user', content: text }
    setMessages(prev => [...prev, userMsg])
    setIsLoading(true)

    try {
      const res = await enviarMensagem(text, messages)
      const assistantMsg: ChatMessage = { role: 'assistant', content: res.dados.reply }
      setMessages(prev => [...prev, assistantMsg])
    } catch {
      const errMsg: ChatMessage = {
        role: 'assistant',
        content: 'Erro ao conectar com o assistente. Tente novamente.',
      }
      setMessages(prev => [...prev, errMsg])
    } finally {
      setIsLoading(false)
    }
  }, [messages, isLoading])

  const clearMessages = useCallback(() => setMessages([]), [])

  return { messages, isLoading, sendMessage, clearMessages }
}
```

- [ ] **Step 2: Verificar tipos**

```bash
cd frontend && npx tsc --noEmit 2>&1 | head -20
```

Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/hooks/useChat.ts
git commit -m "feat: add useChat hook for stateless chat state management"
```

---

## Task 10: Criar ChatButton, ChatPanel e Chat.css

**Files:**
- Create: `frontend/src/components/Chat/ChatButton.tsx`
- Create: `frontend/src/components/Chat/ChatPanel.tsx`
- Create: `frontend/src/components/Chat/Chat.css`

- [ ] **Step 1: Criar Chat.css**

```css
/* frontend/src/components/Chat/Chat.css */

/* Botão flutuante */
.chat-button {
  position: fixed;
  bottom: 24px;
  right: 24px;
  width: 52px;
  height: 52px;
  border-radius: 50%;
  background-color: #2563eb;
  color: #fff;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22px;
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.25);
  z-index: 1000;
  transition: background-color 0.2s;
}

.chat-button:hover {
  background-color: #1d4ed8;
}

/* Painel de chat */
.chat-panel {
  position: fixed;
  bottom: 88px;
  right: 24px;
  width: 360px;
  height: 500px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.18);
  display: flex;
  flex-direction: column;
  z-index: 1000;
  animation: chat-slide-in 0.2s ease-out;
  overflow: hidden;
}

@keyframes chat-slide-in {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: translateY(0); }
}

.chat-panel__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 16px;
  background-color: #2563eb;
  color: #fff;
  font-size: 14px;
  font-weight: 600;
}

.chat-panel__close {
  background: none;
  border: none;
  color: #fff;
  font-size: 18px;
  cursor: pointer;
  line-height: 1;
  padding: 0;
}

.chat-panel__messages {
  flex: 1;
  overflow-y: auto;
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.chat-panel__empty {
  color: #9ca3af;
  font-size: 13px;
  text-align: center;
  margin: auto;
}

.chat-msg {
  max-width: 88%;
  padding: 8px 12px;
  border-radius: 10px;
  font-size: 13px;
  line-height: 1.45;
  word-break: break-word;
}

.chat-msg--user {
  background-color: #2563eb;
  color: #fff;
  align-self: flex-end;
  border-bottom-right-radius: 2px;
}

.chat-msg--assistant {
  background-color: #f3f4f6;
  color: #111827;
  align-self: flex-start;
  border-bottom-left-radius: 2px;
}

.chat-panel__input-row {
  display: flex;
  gap: 8px;
  padding: 10px 12px;
  border-top: 1px solid #e5e7eb;
}

.chat-panel__input {
  flex: 1;
  padding: 8px 12px;
  border: 1px solid #d1d5db;
  border-radius: 8px;
  font-size: 13px;
  outline: none;
}

.chat-panel__input:focus {
  border-color: #2563eb;
}

.chat-panel__send {
  padding: 8px 14px;
  background-color: #2563eb;
  color: #fff;
  border: none;
  border-radius: 8px;
  font-size: 13px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.chat-panel__send:hover:not(:disabled) {
  background-color: #1d4ed8;
}

.chat-panel__send:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
```

- [ ] **Step 2: Criar ChatButton.tsx**

```tsx
// frontend/src/components/Chat/ChatButton.tsx
import './Chat.css'

interface ChatButtonProps {
  onClick: () => void
}

export default function ChatButton({ onClick }: ChatButtonProps) {
  return (
    <button className="chat-button" onClick={onClick} aria-label="Abrir assistente">
      💬
    </button>
  )
}
```

- [ ] **Step 3: Criar ChatPanel.tsx**

```tsx
// frontend/src/components/Chat/ChatPanel.tsx
import { useState, useRef, useEffect } from 'react'
import type { ChatMessage } from '../../types'
import './Chat.css'

interface ChatPanelProps {
  messages: ChatMessage[]
  isLoading: boolean
  onSend: (text: string) => void
  onClose: () => void
}

export default function ChatPanel({ messages, isLoading, onSend, onClose }: ChatPanelProps) {
  const [input, setInput] = useState('')
  const bottomRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' })
  }, [messages])

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    const text = input.trim()
    if (!text) return
    onSend(text)
    setInput('')
  }

  return (
    <div className="chat-panel">
      <div className="chat-panel__header">
        <span>Assistente Caixa Diário</span>
        <button className="chat-panel__close" onClick={onClose} aria-label="Fechar chat">✕</button>
      </div>

      <div className="chat-panel__messages">
        {messages.length === 0 && (
          <p className="chat-panel__empty">Como posso ajudar com o Caixa Diário?</p>
        )}
        {messages.map((msg, i) => (
          <div key={i} className={`chat-msg chat-msg--${msg.role}`}>
            {msg.content}
          </div>
        ))}
        {isLoading && (
          <div className="chat-msg chat-msg--assistant">...</div>
        )}
        <div ref={bottomRef} />
      </div>

      <form className="chat-panel__input-row" onSubmit={handleSubmit}>
        <input
          className="chat-panel__input"
          value={input}
          onChange={e => setInput(e.target.value)}
          placeholder="Digite sua pergunta..."
          disabled={isLoading}
          autoFocus
        />
        <button className="chat-panel__send" type="submit" disabled={isLoading || !input.trim()}>
          Enviar
        </button>
      </form>
    </div>
  )
}
```

- [ ] **Step 4: Verificar tipos**

```bash
cd frontend && npx tsc --noEmit 2>&1 | head -20
```

Expected: sem erros.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/Chat/
git commit -m "feat: add ChatButton, ChatPanel and Chat.css"
```

---

## Task 11: Integrar chat no Layout.tsx

**Files:**
- Modify: `frontend/src/components/Layout/Layout.tsx`

- [ ] **Step 1: Atualizar Layout.tsx**

Substituir o conteúdo completo de `frontend/src/components/Layout/Layout.tsx`:

```tsx
import { useState } from 'react'
import type { ReactNode } from 'react'
import TopBar from './TopBar'
import TabsBar from './TabsBar'
import ChatButton from '../Chat/ChatButton'
import ChatPanel from '../Chat/ChatPanel'
import { useChat } from '../../hooks/useChat'
import './Layout.css'

export default function Layout({ children }: { children: ReactNode }) {
  const [chatOpen, setChatOpen] = useState(false)
  const { messages, isLoading, sendMessage, clearMessages } = useChat()

  const handleClose = () => {
    setChatOpen(false)
    clearMessages()
  }

  return (
    <>
      <TopBar />
      <TabsBar />
      <div className="tab-content">{children}</div>
      <ChatButton onClick={() => setChatOpen(o => !o)} />
      {chatOpen && (
        <ChatPanel
          messages={messages}
          isLoading={isLoading}
          onSend={sendMessage}
          onClose={handleClose}
        />
      )}
    </>
  )
}
```

- [ ] **Step 2: Rodar todos os testes do frontend**

```bash
cd frontend && npx vitest run 2>&1 | tail -15
```

Expected: todos os testes passando.

- [ ] **Step 3: Build de produção**

```bash
cd frontend && npm run build 2>&1 | tail -10
```

Expected: `✓ built in ...` sem erros.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/Layout/Layout.tsx
git commit -m "feat: integrate ChatButton and ChatPanel into Layout"
```

---

## Task 12: Build final e smoke test manual

- [ ] **Step 1: Build completo backend**

```bash
dotnet build CaixaDiario.API/CaixaDiario.API.csproj -c Release 2>&1 | tail -5
```

Expected: `Build succeeded.`

- [ ] **Step 2: Rodar todos os testes backend**

```bash
dotnet test CaixaDiario.Tests -v minimal 2>&1 | tail -10
```

Expected: todos os testes passando, sem falhas.

- [ ] **Step 3: Verificar que ANTHROPIC_API_KEY está no .env**

Confirmar que `.env` contém uma chave real (não vazia) na linha `ANTHROPIC_API_KEY=`.

- [ ] **Step 4: Subir o app localmente e testar manualmente**

```bash
dotnet run --project CaixaDiario.API/CaixaDiario.API.csproj
```

Verificar:
1. Login na aplicação
2. Botão 💬 aparece no canto inferior direito em todas as páginas
3. Clicar abre o painel "Assistente Caixa Diário"
4. Pergunta válida (ex: "como adiciono uma entrada?") → recebe resposta da IA
5. Pergunta off-topic (ex: "receita de bolo") → recebe mensagem de bloqueio sem chamar a API
6. Fechar o painel limpa o histórico

- [ ] **Step 5: Commit final**

```bash
git add -A
git commit -m "feat: complete AI chat with guard-rails (Claude Haiku 3.5)"
```
